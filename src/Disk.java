import java.util.Arrays;
import java.util.List;

public class Disk {
    // Допустим на нашу ленту помещается 2 чанка
    public static int SIZE = 2;
    public static List<Chunk> tape = Arrays.asList(new Chunk[SIZE]);
}
